import { ActionStepSection } from "@/components/sections/ActionStepSection";

export const metadata = {
  title: "STAGE ACTION: Langkah Kecil — UNMASKED",
};

export default function ActionStepPage() {
  return <ActionStepSection />;
}
