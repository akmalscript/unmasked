import { StoryReflectionSection } from "@/components/sections/StoryReflectionSection";

export const metadata = {
  title: "LOAD: Analisis Cerita — UNMASKED",
};

export default function StoryReflectionPage() {
  return <StoryReflectionSection />;
}
